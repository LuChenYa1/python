"""

1.len()  # 长度
2.max()  # 最大元素
3.min()  # 最小元素
4.转换
(1)str(容器)
(2)set(容器)
(3)tuple(容器)
(4)list(容器)
没有转换字典函数
字典转换其他容器会把value舍弃，将key作为元素,但转换为字符串可保留value
其他容器转字符串会保留[]\ ()\ {}
转换为集合会自动去重

5.sorted(容器, [reverse = True])一般默认正向排序，且会自动转为列表

"""
